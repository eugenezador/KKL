pyserial
# pyinstaller KKL.py --onefile

# append 
# QT_QPA_PLATFORM=wayland
# to
# sudo nano /etc/environment

# install before build
# sudo apt install software-properties-qt

# and txt to dist folder
